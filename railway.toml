[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Disable healthcheck temporarily until app is properly configured
healthcheckPath = ""
startCommand = "java -Xmx512m -jar app.war"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
